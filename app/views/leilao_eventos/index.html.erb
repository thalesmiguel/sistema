<div class="card p-t-0 p-b-70 p-l-30 p-r-30">

  <div id="leilao_evento_modal" class="modal bottom-sheet modal-grande"></div>

  <div class="row m-b-0">
    <div class="valign-wrapper">
      <div class="col s6 valign">
        <h3>Eventos</h3>
      </div>
      <div class="col s6 valign right-align">
        <%= link_to_new new_leilao_evento_path if can? :manage, :leilao_evento %>
      </div>
    </div>
  </div>
  <table id="leilao_eventos-table" class="highlight bordered" data-source="<%= leilao_eventos_path(format: :json) %>">
    <thead>
      <tr>
        <th width="50%">Nome <%= filtro_dt %> </th>
        <th width="25%">Data Início <%= filtro_dt %> </th>
        <th width="25%">Data Fim <%= filtro_dt %> </th>
      </tr>
    </thead>
    <tbody>
    </tbody>
  </table>

</div>
