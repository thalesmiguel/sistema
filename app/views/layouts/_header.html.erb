<nav>
  <div class="nav-wrapper">
    <%= link_to "Sistema<span class='smaller'>Central</span>".html_safe, root_path, class:"brand-logo p-l-15", data:{turbolinks: false} %>

    <%# if user_signed_in? %>
    <% if user_signed_in? %>
      <ul class="right">
        <li><%= link_to '<i class="material-icons">account_circle</i>'.html_safe, users_path, class:"waves-effect" if current_user.has_role? :admin %></li>
        <li><a class="dropdown-button" href="#!" data-beloworigin="true" data-activates="user"><%= current_user.username %></a></li>
      </ul>
      <ul id="user" class="dropdown-content">
        <li class="divider"></li>
        <li><%= link_to('Sair', destroy_user_session_path, :method => :delete) %></li>
      </ul>

      <a href="#" data-activates="mobile-menu" class="button-collapse"><i class="material-icons">menu</i></a>
    <% end %>

    <!-- <%# if user_signed_in? %>

      <ul class="right">
        <%# if user_signed_in? %>
          <li> <%#= link_to('Sair', destroy_user_session_path, :method => :delete) %> </li>
        <%# end %>
      </ul>

      <a href="#" data-activates="mobile-menu" class="button-collapse"><i class="material-icons">menu</i></a>
      <ul class="right hide-on-med-and-down">
        <li><%#= link_to '<i class="material-icons">account_circle</i>'.html_safe, users_path, class:"waves-effect" if current_user.has_role? :admin %></li>
        <li><%#= link_to '<i class="material-icons">add</i>'.html_safe, new_evento_path, remote:true, class:"waves-effect" if current_user.pode_incluir? %></li>
        <li><a data-target="impressao_modal" class="modal-trigger"><i class="material-icons">print</i></a></li>
      </ul>
      <ul class="side-nav" id="mobile-menu">
        <li><%#= link_to 'Usuários', users_path, class:"waves-effect" if current_user.has_role? :admin %></li>
        <li class="center"><%#= link_to 'Novo'.html_safe, new_evento_path, remote:true, class:"waves-effect" if current_user.pode_incluir? %></li>
        <li><a data-target="impressao_modal" class="center modal-trigger">Imprimir</a></li>
      </ul>
    <%# end %> -->
  </div>
</nav>
