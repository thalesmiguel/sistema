elemento = $("#" + "<%= model %>" + "_cidade_id")
elemento.empty().append("<%= escape_javascript(render 'cidades/cidades', cidades: @cidades) %>");
$('select').not('.yadcf-filter').material_select();
