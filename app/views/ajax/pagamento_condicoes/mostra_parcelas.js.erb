Materialize.toast("<%= notice %>", 7000, 'rounded deep-purple');

setTimeout(() =>{
  $("#pagamento_condicao_modal").html("<%= escape_javascript(render partial: 'form' ) %>");
  $("label[for^='pagamento_condicao']").not("label[for='pagamento_condicao_entrada']").addClass('active');
  $('select').not('.yadcf-filter').material_select();
  valida_formulario();
  mascaras();
}, 150);
