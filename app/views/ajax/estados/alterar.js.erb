// elemento = $("#estado_<%= obj.id %>");
modal = $("#estado_modal")

// elemento.replaceWith("<%= escape_javascript(render file: 'estados/_estado.html.erb', locals: { estado: obj }) %>");
modal.closeModal();
valida_formulario();
